const $=id=>document.getElementById(id);const form=$('points-form');const modules=[['social',15,3],['volunteer',15,3],['innovation',15,3],['sports',5,1],['labor',2.5,5]];function update(){const other=$('year').value==='other';for(const [id]of modules)$(id).disabled=other;$('example').disabled=other;if(other||!form.checkValidity()){for(const[id]of modules)$(id+'-out').textContent='—';$('total').textContent='—';$('remaining').textContent='—';$('points-status').textContent=other?'该年级未提供适用换算规则。':'请填写非负、范围内的有效积分。';$('points-tip').textContent=other?'请先获取本年级正式细则，不直接沿用 2024、2025 级参考值。':'请修正输入后再查看结果。';$('points-bar').style.width='0%';return}let total=0,full=0;for(const[id,cap,score]of modules){const value=+$(id).value;const points=Math.min(value/cap,1)*score;total+=points;if(value>=cap)full++;$(id+'-out').textContent='+'+points.toFixed(2)}$('total').textContent=total.toFixed(2);$('remaining').textContent=(15-total).toFixed(2)+' 分';$('points-bar').style.width=(total/15*100)+'%';$('points-status').textContent=`已达到参考上限：${full} / 5 类。`;$('points-tip').textContent=full===5?'五类已达到参考上限，超额积分不跨模块抵扣；获奖仍需完整评定。':'先确认活动所属模块及认定学期，再安排参与时间。'}form.addEventListener('input',update);$('year').addEventListener('change',update);$('example').addEventListener('click',()=>{for(const[id,cap]of modules)$(id).value=cap;update()});form.addEventListener('reset',()=>setTimeout(update,0));update();
